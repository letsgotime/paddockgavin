Drop teaser.mp4 here and the block on /events/pistonpoweredranch plays it.

16:9 landscape, H.264, under about 30MB so it starts fast on a phone.
Until the file exists the poster image carries the block on its own, so a
missing video never breaks the page.
